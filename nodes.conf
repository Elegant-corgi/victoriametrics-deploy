# 每行格式：IP roles=逗号分隔的角色
# 必须恰好三台 vmstorage；至少两台 vminsert/vmselect；恰好两台 vmagent/vmalert/vmauth。
10.27.3.68 roles=vmstorage,vminsert,vmselect,vmagent,vmalert,vmauth
10.27.3.69 roles=vmstorage,vminsert,vmselect,vmagent,vmalert,vmauth
10.27.3.70 roles=vmstorage,vminsert,vmselect
